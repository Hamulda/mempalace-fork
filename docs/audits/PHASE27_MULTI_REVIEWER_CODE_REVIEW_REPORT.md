# Code Review Report: mempalace — PHASE27 Open Items

**Target:** `~/.claude/plugins/marketplaces/mempalace`
**Scope:** 68 source files, 135 test files, 10-commit diff
**Reviewers (9 dimensions):** Security, Performance, Architecture, Testing, Accessibility, Bottlenecks, Memory Leaks, Python 3.14+, Creative/Specialist
**Date:** 2026-04-30
**Status:** Active items updated after PHASE36 sprint fixes.

---

## Critical — 0

*(None)*

---

## High — 0

*(ARCH-004, ARCH-005 fixed in PHASE36 sprint)*

---

## Medium — 1

| ID | File:Line | Description | Status |
|----|-----------|-------------|--------|
| CRE-004 | all `.py` files | Zero opentelemetry instrumentation | **Skipped** — requires design decision; deferred per user decision |

---

## Low — 30

**Coverage gaps:** No embed_daemon IPC tests, no LanceDB error path tests (corrupted/missing collection, dimension mismatch), no miner batch commit verification, no MemoryGuard startup race tests.

---

## Summary Table

| Dimension | Critical | High | Medium | Low | Total |
|-----------|----------|------|--------|-----|-------|
| Architecture | 0 | 0 | 0 | 3 | 3 |
| Creative/Specialist | 0 | 0 | 1 | 4 | 5 |
| Testing | 0 | 0 | 0 | 5 | 5 |
| Security | 0 | 0 | 0 | 6 | 6 |
| Accessibility | 0 | 0 | 0 | 7 | 7 |
| Performance | 0 | 0 | 0 | 3 | 3 |
| Memory Leaks | 0 | 0 | 0 | 1 | 1 |
| Python 3.14+ | 0 | 0 | 0 | 3 | 3 |
| **Total** | **0** | **0** | **1** | **30** | **31** |

---

## PHASE36 Fixes Applied

### ARCH-004 (High → Fixed)
`SemanticDeduplicator` no longer leaks `ThreadPoolExecutor` to callers.

**Changes:**
- `BaseCollection.classify_batch()` — new abstract method (base.py:157-189)
- `LanceCollection.classify_batch()` — new implementation with ThreadPoolExecutor internal (lance.py:1589-1676)
- `LanceCollection._dedup` — `SemanticDeduplicator` instance at `__init__` (lance.py:1517)
- `SemanticDeduplicator.classify_batch()` — now takes abstract `BaseCollection` (lance.py:851-943)
- `_do_add` and `upsert` use `self.classify_batch()` instead of direct `SemanticDeduplicator` instantiation

### ARCH-005 (High → Fixed)
`LanceOptimizer` no longer tightly coupled to raw `LanceCollection` lifecycle.

**Changes:**
- `BaseCollection.optimize()` — new abstract method (base.py:191-193)
- `LanceCollection.optimize()` — delegates to `LanceOptimizer.run_optimize_sync()` (lance.py:1534-1538)
- `LanceOptimizer` operates on `(palace_path, collection_name)` strings — no raw `LanceCollection` reference

### ARCH-006 (Medium → Fixed)
`_commit_batch` feature envy eliminated — logic moved to `BaseCollection`.

**Changes:**
- `BaseCollection.upsert_with_tombstones()` — new abstract method (base.py:191-209)
- `BaseCollection.get_by_source_files()` — new abstract method (base.py:211-224)
- `LanceCollection.upsert_with_tombstones()` — implementation (lance.py:1544-1603)
- `LanceCollection.get_by_source_files()` — implementation (lance.py:1605-1635)
- `_commit_batch()` refactored to call collection methods (miner.py:1244-1374)

---

## Still Open

1. **CRE-004**: OpenTelemetry zero instrumentation — **Skipped** (requires design decision; user deferred)

*9/9 dimensions reviewed. Generated by 9-agent parallel review team.*
